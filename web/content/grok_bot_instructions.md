# Grok-bot instructies

Dit bestand gaat **niet** mee in de webhook. De bot heeft zijn eigen instructies.
De webhook stuurt `type` (`new-ticket`, `ticket-solved`, `user-reply` of `ticket-reopened`), `ticket_id`, `api_key` (max. 1 uur) en `send_key`.
